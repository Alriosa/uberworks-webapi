This folder holds uploaded report images (see ReportsController.Create). Its actual
contents are git-ignored (see the .gitignore rule mirroring professional-photos); this file
only exists so the empty folder itself stays tracked.
